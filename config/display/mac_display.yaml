name: "Mac Display"
unique_id: "mac_display_light"
command_topic: sucicada/home/light/mac_display/cmd
state_topic: sucicada/home/light/mac_display/state

# JSON 模式：开启亮度，HA 才显示亮度滑块（设备侧可恒定 100）
schema: json
brightness: true
brightness_scale: 100

# QoS和优化设置
qos: 1
optimistic: false  # 等待状态确认；若一直是闪电按钮，让设备 retain 发布 {"state":"OFF","brightness":100}
retain: true       # 保留消息，确保状态持久化
